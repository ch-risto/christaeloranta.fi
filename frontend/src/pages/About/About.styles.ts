import styled from 'styled-components';
import { BaseHero } from '../../components/common/BaseComponents.styles';

export const AboutMain = styled.main`
    width: 100%;
`;

export const AboutHero = styled(BaseHero)`
    gap: 0;
    max-width: 75%;
`;
