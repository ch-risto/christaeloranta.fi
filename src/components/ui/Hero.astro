---
interface HeroProps {
  title: string;
  description: string;
  imageUrl?: string;
  type?: "main" | "secondary";
}
const { imageUrl, type = "main" } = Astro.props as HeroProps;

const headerType = {
  main: {
    container: "h-dvh justify-center",
    title: "md:text-9xl text-6xl",
    description: "",
    tags: "flex flex-row flex-wrap tracking-wider gap-2 mb-2",
  },
  secondary: {
    container: "md:h-120 h-80 justify-end",
    title: "md:text-8xl  text-4xl",
    description: "",
    tags: "bg-brand-highlight-secondary/50",
  },
};

const header = headerType[type];
---

<section
  class={`w-full grow bg-brand-background flex flex-col items-center ${header.container} p-4 relative`}
>
  {
    imageUrl && (
      <>
        <div class="absolute inset-0 pointer-events-none" />
        <div
          class="absolute inset-0 pointer-events-none"
          style={`background-image: url(${imageUrl}); background-repeat: repeat; background-size: 65%; filter: brightness(0) saturate(100%) invert(35%) sepia(8%) saturate(3258%) hue-rotate(138deg) brightness(102%) contrast(85%); opacity: 0.85;`}
        />
      </>
    )
  }
  <div
    class="w-full max-w-6xl justify-start bg-surface-primary/80 md:bg-transparent md:bg-[linear-gradient(to_right,var(--color-surface-primary)_10%,transparent_70%)] rounded-lg p-4 md:py-8 relative z-10"
  >
    <div
      class={`${header.tags} motion-safe:animate-[fade-in-up_0.5s_ease-out_both]`}
    >
      <slot name="tags" />
    </div>
    <h1
      class={`font-serif text-heading-primary mb-2 max-w-2xl ${header.title} motion-safe:animate-[fade-in-up_0.55s_ease-out_both] anim-delay-1`}
    >
      <slot name="title" />
    </h1>
    <p
      class={`font-sans-secondary uppercase text-content-tertiary tracking-tight text-sm md:text-base leading-6 md:leading-7 md:max-w-md md:px-2 ${header.description} motion-safe:animate-[fade-in-up_0.55s_ease-out_both] anim-delay-2`}
    >
      <slot name="description" />
    </p>
    <div
      class="mt-4 motion-safe:animate-[fade-in-up_0.55s_ease-out_both] anim-delay-3"
    >
      <slot name="additional-content" />
    </div>
  </div>
</section>
